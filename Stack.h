#ifndef STACK_H
#define STACK_H

#include <iostream>
#include <vector>
using namespace	std;

class stack
{
	private:
	vector<int> data; //declaring the vector

	public:
	stack(); //needed functions for the class
	bool.isEmpty();
	void insert(const int& item);
	void remove();
	int top();
	double avg();
};

#endif